extends layout

block content
  h1= title
  p Enter a new project
  form(name="newproject", action="/projects", method="post")
    input(type="text", name="name")
    input(type="text", name="counter")
    input(type="submit", value="submit")
  p Back to 
    a(href='../projects') list
